/**
 * ملف: true-false.types.ts
 * الغرض: تعريفات الأنواع (TypeScript Types) الخاصة بوحدة لعبة "صح أو
 * خطأ" فقط. هذا الملف مسؤول فقط عن "شكل" بيانات السؤال، ولا يحتوي أي
 * منطق تنفيذي — بنفس نمط quiz.types.ts تمامًا.
 */

/**
 * شكل سؤال "صح أو خطأ" الواحد كما يُخزَّن في questions.json وكما يُرجَع
 * في استجابات الـ API.
 *
 * - id: رقم فريد يميّز كل سؤال (لا يتكرر بين الأسئلة). **مخزَّن يدويًا
 *   داخل questions.json نفسه** (على عكس quiz.types.ts حيث يُولَّد
 *   تلقائيًا وقت التحميل)، حتى يبقى ثابتًا تمامًا حتى مع استيراد أسئلة
 *   جديدة أو إعادة ترتيب الملف مستقبلًا. عند إضافة أسئلة جديدة، يجب
 *   إعطاء كل سؤال جديد id فريدًا لم يُستخدم من قبل (عادة: أكبر id
 *   موجود + 1). هذا يجعله مناسبًا لمنع تكرار الأسئلة عبر البوت (تمرير
 *   id في random-exclude) وكذلك لمنع تكرار الأسئلة وقت الاستيراد نفسه.
 * - question: نص السؤال (يدعم العربية وUTF-8 بالكامل).
 * - answer: الإجابة الصحيحة الوحيدة لهذا السؤال، إما true (صح) أو false
 *   (خطأ). على عكس الكويز، سؤال "صح أو خطأ" له إجابة منطقية واحدة فقط،
 *   وليس مصفوفة إجابات نصية.
 * - category: تصنيف موضوعي للسؤال (مثل "علوم"، "جغرافيا"، "تاريخ"...).
 */
export interface TrueFalseQuestion {
  id: number;
  question: string;
  answer: boolean;
  category: string;
}
