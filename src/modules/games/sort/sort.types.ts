/**
 * ملف: sort.types.ts
 * الغرض: تعريفات الأنواع (TypeScript Types) الخاصة بوحدة لعبة "رتب"
 * فقط. هذا الملف مسؤول فقط عن "شكل" بيانات عنصر اللعبة، ولا يحتوي أي
 * منطق تنفيذي - بنفس نمط quiz.types.ts وriddles.types.ts تمامًا.
 */

/**
 * شكل عنصر "رتب" الكامل كما يُخزَّن في questions.json، وكما يُرجَع في
 * كل استجابات الـ API (بما فيها `random` و`random-exclude`) - بنفس
 * أسلوب Quiz API وEye API وRiddles API تمامًا: answers ترجع دائمًا.
 *
 * - id: رقم صحيح موجب فريد يميّز كل سؤال. **مخزَّن مسبقًا داخل
 *   questions.json نفسه** (نفس أسلوب eye.types.ts وriddles.types.ts)،
 *   وليس مولَّدًا تلقائيًا وقت التحميل. عند إضافة أسئلة جديدة، يجب
 *   إعطاء كل سؤال id فريدًا لم يُستخدم من قبل.
 * - questions: مصفوفة تحتوي أكثر من "ترتيب عشوائي" لنفس حروف الكلمة
 *   الصحيحة (كل عنصر فيها نص واحد بصيغة حروف مفصولة بمسافات، مثل
 *   "ن ا ج ي ن د ا"). عند اختيار سؤال عشوائي (`random` أو
 *   `random-exclude`) يتم اختيار **عنصر واحد فقط** من هذه المصفوفة
 *   وإرساله للمستخدم كحقل `question` مفرد - وليس المصفوفة كاملة.
 * - answers: مصفوفة الإجابات المقبولة (الكلمة الصحيحة بدون تبعثر)،
 *   بنفس نمط answers في Quiz API. قد تحتوي أكثر من إجابة صحيحة مستقبلًا
 *   (تهجئات مختلفة لنفس الاسم).
 */
export interface SortQuestion {
  id: number;
  questions: string[];
  answers: string[];
}

/**
 * شكل عنصر "رتب" كما يُرجَع تحديدًا في `random` و`random-exclude`.
 *
 * هذا الشكل مختلف عمدًا عن SortQuestion الكامل:
 *   - question: نص مفرد واحد فقط، مُختار عشوائيًا من مصفوفة questions
 *     الأصلية (وليس المصفوفة كاملة).
 *   - answers: غير موجود إطلاقًا هنا (محذوف عمدًا) حتى لا تُكشَف
 *     الإجابة الصحيحة للبوت المستهلك أثناء اللعب الفعلي.
 */
export interface SortRandomItem {
  id: number;
  question: string;
  answers: string[];
}
