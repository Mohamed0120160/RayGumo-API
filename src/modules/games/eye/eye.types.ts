/**
 * ملف: eye.types.ts
 * الغرض: تعريفات الأنواع (TypeScript Types) الخاصة بوحدة لعبة "عين"
 * فقط. هذا الملف مسؤول فقط عن "شكل" بيانات عنصر اللعبة، ولا يحتوي أي
 * منطق تنفيذي - بنفس نمط riddles.types.ts وtrue-false.types.ts تمامًا.
 */

/**
 * شكل عنصر "عين" الواحد كما يُخزَّن في questions.json وكما يُرجَع في
 * استجابات الـ API.
 *
 * - id: رقم صحيح موجب فريد يميّز كل عنصر. **مخزَّن مسبقًا داخل
 *   questions.json نفسه** (نفس أسلوب riddles.types.ts وtrue-false.types.ts)،
 *   مرقّم تسلسليًا (1، 2، 3...) حسب ترتيب العنصر في الملف وقت إنشائه.
 *   عند إضافة عناصر جديدة، يجب إعطاء كل عنصر id فريدًا لم يُستخدم من
 *   قبل (عادة: أكبر id موجود حاليًا + 1). هذا الحقل مهم جدًا لأن بوت
 *   الواتساب يعتمد عليه في نظام منع التكرار (anti-repeat) عبر إرسال
 *   قائمة الـ id المستخدمة سابقًا إلى `random-exclude`.
 * - img: رابط الصورة المعروضة على اللاعب (يدعم أي رابط صورة صالح، مثل
 *   روابط telegra.ph).
 * - name: اسم الشخصية أو العنصر الظاهر في الصورة، وهو الإجابة الصحيحة
 *   الوحيدة المتوقعة من اللاعب (يدعم العربية وUTF-8 بالكامل).
 *
 * ملاحظة: على عكس لعبة الألغاز والكويز، حقل الإجابة هنا (`name`) نص
 * مفرد وليس مصفوفة، لأن مصدر البيانات الحقيقي المزوَّد يحمل اسمًا
 * واحدًا فقط لكل صورة. أي دعم لصياغات إجابة متعددة مستقبلًا يجب أن
 * يأتي من بيانات حقيقية تحمله فعلًا وليس بشكل افتراضي.
 */
export interface EyeItem {
  id: number;
  img: string;
  name: string;
}
