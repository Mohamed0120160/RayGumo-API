/**
 * ملف: quiz.types.ts
 * الغرض: تعريفات الأنواع (TypeScript Types) الخاصة بوحدة الكويز فقط.
 *
 * هذا الملف مسؤول فقط عن "شكل" بيانات سؤال الكويز، ولا يحتوي أي منطق
 * تنفيذي. أي وحدة لعبة مستقبلية (anime، riddles...) يجب أن يكون لها
 * ملف types خاص بها بنفس هذا النمط، منفصل تمامًا عن أنواع الألعاب
 * الأخرى.
 */

/**
 * شكل سؤال الكويز الواحد كما يُخزَّن في questions.json وكما يُرجَع في
 * استجابات الـ API.
 *
 * - id: رقم فريد يميّز كل سؤال (لا يتكرر بين الأسئلة). يُولَّد تلقائيًا
 *   أثناء تحميل البيانات إذا لم يكن موجودًا أصلًا في مصدر البيانات
 *   الخام (انظر quiz.service.ts / loadQuestionsData).
 * - question: نص السؤال (يدعم العربية وUTF-8 بالكامل).
 * - answers: مصفوفة الإجابات الصحيحة المقبولة لهذا السؤال. سؤال واحد قد
 *   يقبل أكثر من إجابة صحيحة (مثل تهجئتين مختلفتين لنفس الاسم)، لذلك
 *   هذا الحقل مصفوفة نصوص دائمًا، وليس نصًا مفردًا أبدًا.
 * - category: تصنيف موضوعي للسؤال (مثل "جغرافيا"، "علوم"، "أنمي"...).
 *
 * ملاحظة: لا يوجد حقل "difficulty" (مستوى صعوبة) في هذا الإصدار، لأن
 * مجموعة البيانات الحقيقية المزوَّدة لا تحتوي هذا الحقل. أي إضافة له
 * مستقبلًا يجب أن تأتي من بيانات حقيقية تحمله فعلًا وليس بشكل افتراضي.
 */
export interface QuizQuestion {
  id: number;
  question: string;
  answers: string[];
  category: string;
}
